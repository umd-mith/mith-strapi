'use strict';

/**
 * taxonomy-value controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::taxonomy-value.taxonomy-value');
